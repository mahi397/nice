import React from 'react'

export default function DayWiseCard() {
  return (
    <div>
      <img></img>
      <div>
        <h2>Day {x}: {portname}</h2>
        <h4>Departs at {departureTime}</h4>
        <p>{description}</p>
      </div>
    </div>
  );
}
